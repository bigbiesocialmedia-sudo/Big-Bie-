rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    // Allow public read access to all files (so customers see images)
    match /{allPaths=**} {
      allow read: if true;
      
      // Only allow uploads/deletes if user is logged in
      allow write: if request.auth != null;
    }
  }
}
